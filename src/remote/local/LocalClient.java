package remote.local;

import io.Client;
import io.Register;

import java.net.InetSocketAddress;

public class LocalClient extends Client {
    public LocalClient(InetSocketAddress proxy, InetSocketAddress bind, Register register) {

    }
}
