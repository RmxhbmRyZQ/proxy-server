package remote.proxy;

import io.Register;
import io.Server;

import java.net.InetSocketAddress;

public class RemoteServer extends Server {
    public RemoteServer(InetSocketAddress dest, InetSocketAddress bind, Register register) {

    }
}
